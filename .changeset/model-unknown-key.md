---
"@nxgt/janus": patch
---

A key other than `relations` and `permissions` on an object type of `defineModel` — `permission:`, singular — is a compile error. It compiled, and was refused only when `defineModel` ran.
