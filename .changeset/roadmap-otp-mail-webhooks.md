---
"@nxgt/janus": patch
---

The roadmap names what comes next: one-time codes (by e-mail, and TOTP as a second factor), sending the e-mails with `@nxgt/janus-mail` and typed, translated default templates you can extend with a language or replace one by one, and signed webhooks. The vocabulary gains a **one-time code** row.
