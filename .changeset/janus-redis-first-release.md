---
"@nxgt/janus-redis": minor
---

The first release: sessions and one-time tokens for `@nxgt/janus` in Redis, over `@nxgt/redis`, expired by Redis itself while users stay in another store. It passes the conformance suite for both stores on Redis 7.4.
