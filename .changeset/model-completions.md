---
"@nxgt/janus": patch
---

`defineModel` is completed by your editor: subject types and subject sets in a relation, subject types in `fromField`, and relations, permissions and arrows in a rule and in `when`. A wrong name is still refused, and the error now lists the names it could have been, with "Did you mean".
